/**
 * Public copy returned by POST /auth/password/forgot — use this in UI.
 * Do not treat it as proof the email exists.
 */
export const FORGOT_PASSWORD_PUBLIC_MESSAGE =
  'If an account exists with a password, we sent a verification code.';
